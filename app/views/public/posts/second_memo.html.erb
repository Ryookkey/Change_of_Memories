<div class="container">
  <div class="row mt-5">
    <h1>Second Memo</h1>
  </div>
  <h5>抽象化を行い、次のメモへ進むための準備をしましょう。</h5>

  <div class="row mt-5 justify-content-center">
    <div class="col-12 text-center">
      <h5>First Memo</h5>
      <p><%= @post.first_memo %></p>
    </div>
  </div>

  <div class="row mt-5 justify-content-center">
    <%= form_with model: @post, url: second_memo_public_post_path(@post), method: :patch, local: true, id: "secondMemoForm" do |form| %>
      <div class="col-8 offset-md-1">
        <h4>Second Memo</h4>
        <%= form.text_area :second_memo, class: 'form-control w-100 mb-4' %>
      </div>

      <h4>公開設定</h4>
      <div class="d-flex">
        <div class="mr-4">
          <%= form.radio_button :second_post_status, true, id: "second_post_status_public" %>
          <label for="second_post_status_public">公開</label>
        </div>
        <div>
          <%= form.radio_button :second_post_status, false, id: "second_post_status_private" %>
          <label for="second_post_status_private">非公開</label>
        </div>
      </div>

      <div class="d-flex justify-content-between mt-3 w-100">
        <%= form.submit '投稿（マイページ）', class: 'btn btn-secondary w-50 mr-2 text-wrap' %>
        <%= hidden_field_tag :next_step, false, id: "nextStepField" %>
        <%= button_tag '次に進む (third memo)', type: 'button', class: 'btn btn-primary w-50', id: "nextStepButton" %>
      </div>
    <% end %>
  </div>
</div>

<script>
  document.getElementById("nextStepButton").addEventListener("click", function() {
    document.getElementById("nextStepField").value = true;
    document.getElementById("secondMemoForm").submit();
  });
</script>
